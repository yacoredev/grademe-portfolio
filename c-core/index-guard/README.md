# index_guard

Repair a lookup so the element is read only after the index is proven in range

```c
int at_or_default(const int *a, size_t n, long i, int fallback)
```

**Difficulty:** 2/5
**Allowed functions:** none

Solved in practice.

[Read the full exercise on Grademe](https://grademe.io/app/exercise/index-guard)
